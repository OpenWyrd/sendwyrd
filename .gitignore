# OS
.DS_Store
Thumbs.db

# Obsidian per-device (don't sync across clones)
.obsidian/workspace.json
.obsidian/workspace-mobile.json
.obsidian/graph.json

# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
dist/
build/
!packages/web/src/app/build/
.eggs/
*.egg
.venv/
venv/
env/

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# Note: plugin binaries and themes ARE tracked so the vault works on clone.
# Run ./setup.sh --force to update all plugins to latest versions.

# Environment
.env
.env.local

# Claude Code / MCP (local credentials — never commit)
.mcp.json
.claude/

# Cloudflare Wrangler local cache
.wrangler/

# OpenNext Cloudflare build output
.open-next/

# Node / pnpm
node_modules/
.pnpm-store/

# Next.js
.next/
out/

# Turborepo
.turbo/

# Build outputs (TS / bundlers)
*.tsbuildinfo

# Test coverage
coverage/

# Environment / secrets (never commit)
.dev.vars
.env.production
.env.development

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Obsidian (per-user editor state, plugin trees) — not for shipping
.obsidian/
