---
type: session
created: 2026-04-26
updated: 2026-04-26
status: completed
last_edited_by: agent_operator
tags: [session, mcp, parallel-session, packages-mcp, npm-published, vision-banked]
tier: 2
branch: feat/mcp-server
pr: 38
npm_package: "@sendwyrd/mcp@0.1.0"
---

# Session — MCP server for SendWyrd

## Goal

Build `packages/mcp/` — a stdio-default Model Context Protocol server that wraps SendWyrd as agent-callable verbs. Closes the agora-replacement vision articulated in the lead-in conversation: SendWyrd is the capsule layer; per-user agents are the routing layer; MCP is the connector that lets every Claude Code / Claude Desktop / Cursor user's agent become a routing node by default.

User instruction: "go ahead and cook on building the whole MCP thing you want." CTO-delegated mode — own the technical and aesthetic calls.

## Parallel-session coordination

User flagged a parallel session active on **IP rate-limiting + UI/UX**. Their surface is `packages/api/` (KV middleware, rate-limit logic) and `packages/web/` (UX iteration). My surface is **`packages/mcp/` only**, all net-new files. Disjoint by construction. The only shared touchpoint is `pnpm-lock.yaml` after `pnpm install` — normal merge artifact, not a real collision.

## Scope declaration

**Will create / modify:**
- `packages/mcp/**` — entire new package
- `pnpm-lock.yaml` — add MCP SDK + workspace link (regenerated by pnpm install)
- `who/governance/VISION.md` — IF user approves the post-agora topology section banked in conversation (currently a CEO call, not yet approved)
- `who/governance/future_horizons.md` — IF user approves H3 entry for agent-routing primitives (same CEO call)
- This session file — heartbeat + SITREP

**Will NOT touch:**
- `packages/api/**` — owned by parallel session
- `packages/web/**` — owned by parallel session
- `packages/core/**` — read-only; if I need an export not present, request via parallel-session-friendly PR ordering rather than push core changes from this branch
- `wrangler.toml` files
- `STATE.md` — defer to whoever closes last
- Any non-MCP infra config (turbo.json, tsconfig.base.json, root package.json — none should need touching)

## Conflict scan

- `git status`: clean
- `how/sessions/active/`: empty before this session (parallel session may write its own file later — no shared file)
- `who/coordination/`: empty
- Branch off `main` at `0fe7267`; parallel session presumably has its own branch

## Plan

1. Scaffold `packages/mcp/` (package.json, tsconfig, src/ tree, install `@modelcontextprotocol/sdk`)
2. Config + seed-store on disk (XDG `~/.config/sendwyrd/`, interop with web's seedStore v2 format)
3. HTTP client (publish, fetch, burn, presence-check, fetch-replies, submit-reply)
4. MCP server entrypoint + tool registration (stdio)
5. Tools: status, init, unlock, lock, compose, view, burn, reply, attest, history, inbox, recover
6. Tests (vitest) + typecheck + smoke against production
7. Docs (`packages/mcp/README.md`)
8. PR

## Design decisions (CTO-delegated)

- **Stdio default; no remote MCP in v1.** Local stdio matches host-blind posture: keys never leave the machine. Remote SSE would require server-side seed handling, which violates ADR-003. Hosted MCP can ship later as a thin courtesy wrapper that *only* exposes view/unfurl (no key-bearing operations).
- **Open seed mode is default** (matches web `feedback_zero_friction_default`). Passphrase opt-in via `SENDWYRD_PASSPHRASE` env var or `--passphrase-cmd "pass sendwyrd"` for `pass` / `gpg` integration.
- **Verbs only, no subscriptions.** `sendwyrd_inbox` is a one-shot read, not a polling/streaming surface. No `_watch`, no `_subscribe`, no auto-reply (per `feedback_anti_scope_creep_relay_layer` and ADR-010).
- **Config dir:** `${XDG_CONFIG_HOME:-~/.config}/sendwyrd/` with `config.json`, `seed.{enc,open}.json`, `history.json`. Same encrypted-seed format as the web app (v2 record from `@sendwyrd/core/seedStore`) so a future "import web seed → MCP" UX is trivial.
- **Origin override:** `SENDWYRD_ORIGIN` env var (default `https://sendwyrd.com`) so dev/staging targets work.
- **No `npm publish` in this PR.** Workspace install only. Publishing to npm under `@sendwyrd/mcp` requires `npm login` against the `@sendwyrd` org which the user has flagged as a separate task in the rolling backlog.

## What shipped

`packages/mcp/` — a stdio MCP server wrapping SendWyrd as 13 agent-callable verbs:

- Lifecycle: `sendwyrd_status`, `sendwyrd_init`, `sendwyrd_unlock`, `sendwyrd_lock`, `sendwyrd_forget`
- Capsule verbs: `sendwyrd_compose`, `sendwyrd_view`, `sendwyrd_burn`, `sendwyrd_reply`, `sendwyrd_attest`
- Read: `sendwyrd_history`, `sendwyrd_inbox`, `sendwyrd_recover`

Files:
- `package.json` — `@sendwyrd/mcp@0.1.0`, bin `sendwyrd-mcp`, esbuild bundle target `node20`
- `tsconfig.json` — NodeNext, ESM, strict, DOM lib (Web Crypto for crypto.subtle / CryptoKey)
- `src/config.ts` — env-driven config; passphrase via `SENDWYRD_PASSPHRASE` or `SENDWYRD_PASSPHRASE_CMD` (no shell expansion)
- `src/store.ts` — disk-backed seed (open + protected modes, interop with web's seedStore v2 record format) + history
- `src/api.ts` — HTTP client (publish, fetch, burn, fetch-replies, submit-reply, presence-check)
- `src/server.ts` — MCP server registration, all tool implementations
- `src/index.ts` — stdio entry with shebang
- `test/store.test.ts` (13 tests) — open + protected seed lifecycle, counter bump, history merge
- `test/config.test.ts` (7 tests) — env-var resolution, CMD precedence, fallthrough on CMD failure
- `vitest.config.ts`
- `scripts/smoke.mjs` — end-to-end against a live origin: init → compose → view → burn → view (gone) → history
- `README.md`

Bundle: 885 KB single-file ESM (zod + MCP SDK + @sendwyrd/core + @noble inlined).

## Verification

- Workspace `pnpm typecheck`: 5/5 green.
- `pnpm test`: 20/20 in mcp; 158/158 core; 105/105 web; total stack green.
- Stdio handshake: `initialize` → `tools/list` returns 13 typed tools.
- E2E smoke against `https://sendwyrd.com`: full init/compose/view/burn/view-gone/history cycle passed; round-tripped body matched, server returned 410 Gone after burn.

## Design decisions banked

- Stdio default; no remote MCP in v1 (would require server-side seed handling — violates ADR-003).
- Open seed mode is default (matches web zero-friction default). Passphrase opt-in.
- Verbs only — no subscriptions, polling, watch, or auto-reply tools (per `feedback_anti_scope_creep_relay_layer` and ADR-010).
- Bundled distribution (esbuild) so the MCP runs as plain Node without workspace-aware module resolution. Consumers can install via npm without needing tsx, ts-node, or the workspace.
- Same on-disk seed format as the web app, so a future "import web seed → MCP" UX is trivial (the encrypted record is bytewise compatible).

## SITREP

### Completed
- All 11 tasks from the plan above. Code, tests, smoke, docs all green.

### In progress
None — ready for PR.

### Next up (post-merge)
- npm publish under `@sendwyrd` (separate task in the rolling backlog; needs `npm login` against the org, which is gated on the user).
- Add `/build` page mention of the MCP. Deferred to a follow-up because the parallel session is touching `packages/web/`; do this after their work merges to avoid stomp.
- Inbox "prove authorship" UI — surfaces the attestation primitive in the web app. Was already in the rolling backlog; the parallel session may already be addressing it (one of their merged commits is `b83f611 feat: in-app "attest authorship" affordance on /inbox`).
- Per-IP rate limiting in api worker via KV — the parallel session is on this (branch `rate-limit-api` in worktree `sendwyrd-rate-limit`).

### Files touched
- Created: everything under `packages/mcp/` (12 files — see "What shipped").
- Modified: `pnpm-lock.yaml` (esbuild + MCP SDK + zod + workspace link).
- This session file.

### Blockers
None.

## Closing addendum (2026-04-26 evening)

Three follow-on commits landed on the same branch / PR #38 after the initial MCP ship:

1. **`4be7dd3` — Vision banked + npm publish prep.** Post-agora topology section added to `who/governance/VISION.md` (per-user agents do the routing the algorithmic agora used to; consequence of the five principles, not a sixth). H3 added to `who/governance/future_horizons.md` (agent-routing surfaces — registry listings, routing-rules DSL, PKM/CRM bridging, inter-agent capsule exchange, local-first SDKs). Package.json prepped: `private: false`, `publishConfig.access: "public"`, runtime deps moved to devDependencies (esbuild bundles them inline).

2. **`a65ed34` — Resources + ergonomic attest + integration tests + STATE refresh.**
   - **MCP Resources:** `sendwyrd-mcp://wyrd/{handle}` exposed via standard `resources/list` + `resources/read` handlers. Live wyrds in local history surface in Claude Code's resource sidebar; reads fetch+decrypt with the locally-stored K_read.
   - **Ergonomic attest:** `target_handle` is now optional on `sendwyrd_attest`. When omitted, picks the most-recent non-attestation, non-burned wyrd from history. New `is_attestation` flag on HistoryEntry distinguishes own-attestation entries from ordinary wyrds.
   - **Integration tests:** 16 tests at `packages/mcp/test/integration/server.integration.test.ts`. Spawns the actual `dist/index.js` bundle, drives JSON-RPC over stdio against a Node-built-in mock HTTP API on port 0. All 13 tools exercised end-to-end. Catches stdio framing / schema validation / error propagation regressions.
   - **STATE.md refreshed:** brought current to 2026-04-26 reality — Tier-1+2 closed, post-agora topology phase, ADR-021, attestation primitive, MCP, active branches in flight (rate-limit-api, security-headers).

3. **`<pending-this-commit>` — README polish + session close.** `packages/mcp/README.md` rewritten with `npx -y @sendwyrd/mcp` as the primary install path (workspace dev demoted to a section below). This session file moved from active/ to history/.

## npm publish

`@sendwyrd/mcp@0.1.0` published to https://registry.npmjs.org/@sendwyrd/mcp on 2026-04-26 23:55 UTC by `deltaclimbs <***>`.

- License: MIT
- Bundle: 192.7 KB tarball, 927.2 KB unpacked (single ESM file targeting node20)
- Bin: `sendwyrd-mcp`
- Deps (runtime): none — esbuild inlines `@sendwyrd/core` + `@modelcontextprotocol/sdk` + `zod` + `@noble/*` + `@scure/*`

Verified end-to-end: `npx -y @sendwyrd/mcp@0.1.0` fetches from the registry, runs, completes the MCP `initialize` handshake, and registers all 13 tools.

Auth method used: granular access token with bypass-2FA (`sendwyrd-publish`, 7-day expiry). User flagged for rotation post-publish; future publishes will need a fresh token (or transition to GitHub Actions Trusted Publishing via OIDC).

## Final SITREP

### Completed
- `@sendwyrd/mcp@0.1.0` published to npm and verified working via `npx`.
- Post-agora topology canonized in VISION.md.
- H3 (agent-routing surfaces) banked in future_horizons.md.
- MCP Resources support added (browsable wyrds in Claude Code sidebar).
- Ergonomic `sendwyrd_attest` with optional target_handle.
- 16-test integration suite spawning the real bundle.
- STATE.md refreshed.
- README rewritten for the npm-first install path.
- All four commits on `feat/mcp-server` (PR #38). Awaiting user merge.

### In progress
None.

### Next up
- **User**: revoke the publish token at https://www.npmjs.com/settings/deltaclimbs/tokens (security hygiene).
- **User**: merge PR #38 (squash recommended — orphan `c431f9f` commit elides automatically).
- `/build` page mention of the MCP — deferred follow-up (the parallel UI/UX session is still touching `packages/web/`).
- MCP registry listings (Anthropic, Smithery) — once the npm package settles.
- Soft-launch — pick 5-10 humans, send wyrd URLs, watch `/ops/{secret}`.
- Future publishes (0.1.1+) need a fresh granular token at publish time, OR a GitHub Actions Trusted Publishing setup.

### Blockers
None.

### Files touched (full session, all four commits)
- `packages/mcp/` — net new package: package.json, tsconfig.json, README.md, vitest.config.ts, src/{index,server,config,store,api}.ts, test/{store,config}.test.ts, test/integration/server.integration.test.ts, scripts/smoke.mjs
- `pnpm-lock.yaml`
- `who/governance/VISION.md` — post-agora topology section
- `who/governance/future_horizons.md` — H3 entry
- `STATE.md` — refresh
- `how/sessions/active/ → history/2026-04/` — this file moves on close

## Next Session Prompt

`@sendwyrd/mcp@0.1.0` is live on npm. PR #38 is open with all the work; merge when ready (squash recommended). Three follow-ups are queued in `STATE.md`'s rolling backlog: revoke the publish token, mention the MCP on the `/build` page, list it in MCP registries (Anthropic + Smithery). Read `packages/mcp/README.md` for the now-current install path and `who/governance/VISION.md` § Post-Agora Topology for the strategic frame the MCP exists to serve. The next concrete H3 surface candidate is an agent-callable routing-rules DSL — defer until at least one user is improvising routing logic on top of the bare verbs.

